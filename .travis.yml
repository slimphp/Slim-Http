sudo: false

language: php

php:
  - 7.1
  - 7.2
  - nightly

matrix:
  allow_failures:
    - php: nightly

before_script:
  - composer require satooshi/php-coveralls:^0.7 squizlabs/php_codesniffer:^2.5 -n ; fi
  - composer install -n ; fi

script:
  - mkdir -p build/logs && phpunit --coverage-clover build/logs/clover.xml ; fi
  - vendor/bin/phpunit ; fi
  - vendor/bin/phpcs ; fi
  - vendor/bin/phpstan analyse -l 3 src/

after_script:
  - php vendor/bin/coveralls -v ; fi
