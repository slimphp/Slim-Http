sudo: false

language: php

php:
  - 7.1
  - 7.2
  - nightly

matrix:
  allow_failures:
    - php: nightly

before_script:
  - composer require satooshi/php-coveralls:^0.7 squizlabs/php_codesniffer:^2.5 -n
  - composer install -n

script:
  - mkdir -p build/logs && phpunit --coverage-clover build/logs/clover.xml
  - vendor/bin/phpunit
  - vendor/bin/phpcs
  - vendor/bin/phpstan analyse -l 3 src/

after_script:
  - php vendor/bin/coveralls -v
